services:
  mongodb:
      image: mongo:7.0.39-jammy

      container_name: mongodb

      ports:
        - "27000:27017"

      volumes:
        - "./mongodb:/data/db"

      networks:
        - backend

      restart: always

networks:
  backend: